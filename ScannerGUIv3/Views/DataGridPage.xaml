<Page
    x:Class="ScannerGUIv3.Views.DataGridPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <controls:DataGrid
            AutoGenerateColumns="False"
            GridLinesVisibility="Horizontal"
            ItemsSource="{x:Bind ViewModel.Employees, Mode=OneWay}"
            IsReadOnly="True">
            <controls:DataGrid.Resources>
                <SolidColorBrush x:Key="DataGridColumnHeaderBackgroundColor" Color="Transparent" />
            </controls:DataGrid.Resources>
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Binding="{Binding EmployeeNumber}" Header="Employee Number" />
                <controls:DataGridTextColumn Binding="{Binding Name}" Header="Name" />
                <controls:DataGridTextColumn Binding="{Binding ShiftType}" Header="Shift Type" />
                <controls:DataGridTextColumn Binding="{Binding SignInTime}" Header="Sign In Time" />
                <controls:DataGridTextColumn Binding="{Binding SignOutTime}" Header="Sign Out Time" />
                <!--<controls:DataGridTextColumn Binding="{Binding ArrivalDate}" Header="Arrival Date" />-->
                <!--<controls:DataGridTextColumn Binding="{Binding DepartureDate}" Header="Departure Date" />-->
            </controls:DataGrid.Columns>
        </controls:DataGrid>
    </Grid>
</Page>