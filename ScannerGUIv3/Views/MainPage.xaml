<Page
    x:Class="ScannerGUIv3.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <!-- First column takes twice the width of the second -->
            <ColumnDefinition Width="*"/>
            <!-- Second column takes the remaining space -->
        </Grid.ColumnDefinitions>


        <Button x:Name="signInButton" Click="signInButton_Click"  Grid.Row="0" Grid.Column="0">Sign In</Button>
        
        <TextBox x:Name="PersonnelNumberTextBox" Header="" PlaceholderText="Personnel Number" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"/>


    </Grid>
</Page>
